__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.venv/

# Default local run state written by `fandea run` (checkpoints, op ledger, provenance ledger,
# workspace snapshots) — never checked in, per M0's own default of a repo-local .fandea/ dir.
.fandea/

